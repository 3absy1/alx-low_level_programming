#include "main.h"

/**
 * _isdigit - check if input is digit
 *
 * @d: input
 *
 * Return: 1if is digit, 0 if not
*/

int _isdigit(int d)
{
	if (c >= 48 && c <= 57)
		return (1);
	else
		return (0);
}
